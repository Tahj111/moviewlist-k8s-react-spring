apiVersion: v1
kind: Service
metadata:
  name: spring-service
  labels:
    app: spring-microservice
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: spring-microservice
